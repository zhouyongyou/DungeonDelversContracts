{
  "name": "dungeondelvers-marketplace-subgraph",
  "version": "1.0.0",
  "description": "DungeonDelvers Marketplace Subgraph",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:studio": "graph deploy --studio dungeondelvers-marketplace",
    "deploy:hosted": "graph deploy --product hosted-service USERNAME/dungeondelvers-marketplace",
    "create:local": "graph create --node http://localhost:8020/ dungeondelvers-marketplace",
    "remove:local": "graph remove --node http://localhost:8020/ dungeondelvers-marketplace",
    "deploy:local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dungeondelvers-marketplace",
    "update:addresses": "node scripts/update-addresses.js",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.51.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}